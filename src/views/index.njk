{% extends "_layout.njk" %}

{% block content %}
  {# <section class="container bg-grey">
    <section class="section-introduction grid-hero grid-960">

      <p>An app for creating a record of all the things on a timeline.</p>

      <p><a href="#" class="btn btn-primary btn-lg">Sign Up!</a> <a class="btn btn-lg" data-bind="click: loginUser">Log In</a></p>

    </section>
  </section> #}

  <section class="section-timelines grid-960" data-bind="foreach: categories">
    <div class="timeline-category">
      <h2 data-bind="text: name, style: { color: color }"></h2>
      <!-- ko if: events().length > 0 -->
      <ul class="timeline" data-bind="foreach: events">
        <!-- ko ifnot: isDeleted -->
        <li class="timeline-event">
          <span class="timeline-event--date" data-bind="text: dateString"></span>
          <span class="timeline-event--name" data-bind="text: name"></span>
          <span class="timeline-event--description" data-bind="text: description"></span>
        </li>
        <!-- /ko -->
      </ul>
      <!-- /ko -->
      <!-- ko ifnot: events().length > 0 -->
      <a href="#" data-bind="click: addEvent">Add an event</a>
      <!-- /ko -->
    </div>
  </section>
{% endblock %}
